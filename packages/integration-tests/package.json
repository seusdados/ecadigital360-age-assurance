{
  "name": "@agekey/integration-tests",
  "version": "0.0.1",
  "private": true,
  "description": "Integration / cross-tenant RLS tests for AgeKey. Skip-friendly when SUPABASE_TEST_URL not set.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:rls": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@agekey/shared": "workspace:*",
    "@supabase/supabase-js": "2.45.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  }
}
