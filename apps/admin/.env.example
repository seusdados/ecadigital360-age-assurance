# =============================================================
# AgeKey admin — environment variables.
# Copy to .env.local for development. NEVER commit .env.local.
# =============================================================

# --- Public (exposed to the browser) ---
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_SUPABASE_URL=https://tpdiccnmsnjtjwhardij.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon-key-here>
NEXT_PUBLIC_AGEKEY_API_BASE=https://tpdiccnmsnjtjwhardij.supabase.co/functions/v1
NEXT_PUBLIC_AGEKEY_ISSUER=https://staging.agekey.com.br

# --- Server-only (never reaches the bundle) ---
# AgeKey Edge Functions admin API key — used by Server Actions.
# This is the dev tenant key from supabase/seed/04_dev_tenant.sql.
# Rotate via the panel before any non-dev usage.
AGEKEY_ADMIN_API_KEY=ak_dev_sk_test_0123456789abcdef
